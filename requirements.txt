# Phase 1: data pipeline (pinned known-good versions)
pandas==2.3.3
numpy==2.2.6
pyarrow==24.0.0
hydra-core==1.3.4
omegaconf==2.3.1
matplotlib==3.10.9
requests==2.34.2
pytest==9.1.1

# Phase 2+ (uncomment when the modules land):
# torch
# pymoo
# shap
# wandb
